"use client";import { useEffect,useState } from "react";import AdminShell from "../../../../../components/admin/AdminShell";import ProjectForm from "../../../../../components/admin/ProjectForm";import { getProject } from "../../../../../services/projectService";export default function Edit({params}){const [item,setItem]=useState();useEffect(()=>{Promise.resolve(params).then(x=>getProject(x.id).then(setItem))},[params]);return <AdminShell><p className="eyebrow">Projects</p><h1>Edit project</h1>{item?<ProjectForm project={item}/>:<p className="muted">Loading project…</p>}</AdminShell>}
